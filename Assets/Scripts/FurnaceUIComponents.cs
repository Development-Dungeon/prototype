using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class FurnaceUIComponents : MonoBehaviour
{
    // slowly add all the parts of the UI here so I don't have to search for them in the other script 
    // everything can be dynamic
    // would it be more smart to have a group where all the text would be aligned to?

    public TMP_Text consumptionRateText; 
    public TMP_Text heatProductionText;
    public TMP_Text furnaceIsOnText;
    public TMP_Text progressText;
}
